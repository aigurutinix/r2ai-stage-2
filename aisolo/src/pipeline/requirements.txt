# Phụ thuộc Python — pipeline sinh bài nộp ViFinQA (Road to AI 2026, Stage 2)
# Python 3.11.0 (bản đã dùng để tạo bài nộp nộp thi)
#
#   python -m venv .venv
#   .venv/Scripts/activate           # Windows;  source .venv/bin/activate trên Linux/macOS
#   pip install -r pipeline/requirements.txt
#
# Chỉ CHẠY LẠI BÀI NỘP thì bốn gói ở nhóm 1 là đủ, chạy trên CPU vài phút.
# Nhóm 2 chỉ cần khi muốn SINH LẠI hai file trung gian (agent_full_v2.json, rerank_cache.json)
# — bước này cần GPU và nhiều giờ; xem pipeline/README.md.

# --- Nhóm 1: bắt buộc để dựng lại bài nộp (CPU) ---
pandas==3.0.5
numpy==2.4.6
pyvi==0.1.1            # tách từ tiếng Việt cho BM25 (bỏ gói này thì điểm retrieval tụt)
certifi==2026.7.22     # chứng chỉ TLS khi gọi endpoint LLM

# --- Nhóm 2: chỉ cần khi sinh lại file trung gian bằng model (GPU) ---
torch==2.6.0+cu124     # bánh xe CUDA: --index-url https://download.pytorch.org/whl/cu124
transformers==5.14.1
sentence-transformers==5.6.1
strands-agents==1.50.2 # khung agent cho agent_strands.py

# --- Nhóm 3: tuỳ chọn, đọc file Excel khi thăm dò dữ liệu ---
openpyxl==3.1.5
